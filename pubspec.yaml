name: softbuilder_dio
description: A custom dio service for Softbuilder Flutter Team.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  dio: ^4.0.0
  shared_preferences: ^2.0.6
  shared_preferences_web: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: